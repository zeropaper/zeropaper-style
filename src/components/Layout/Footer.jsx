import React from 'react';

import Menu from './Menu';

const Footer = () => {
  const classes = {};
  return (
    <footer className={classes.root}>
      <Menu />
    </footer>
  );
};

export default Footer;
